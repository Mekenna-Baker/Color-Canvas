interface ProjectsProps {
    title: string;
    width: number;
    height: number;
    imageData: string;
    assignedUser: {
        username: string;
    };
}

const Projects = ({title, width, height, imageData, username}: ProjectProps) => {
    return (
        <div>
            
        </div>
    )
}
